import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#008069",
      light: "#25d366",
      dark: "#378169",
    },
    secondary: {
      main: "#edf2ff",
    },
  },
});

export { theme };
